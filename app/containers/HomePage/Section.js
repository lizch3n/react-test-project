import styled from 'styled-components';

const Section = styled.section`
  margin: 3em auto;
    width: 45%; 
    float: left;
    position: fixed;
    right: 5em;
    top: 10%;
  &:first-child {
    margin-top: 0;
  }
`;

export default Section;
